---
description: Run brief finish to close a step and continue from its output.
disable-model-invocation: true
allowed-tools: "Bash(brief finish *)"
argument-hint: "<feature> <step> --handoff <path> --state <path>"
---

Run `brief finish $ARGUMENTS` and work from its output.
