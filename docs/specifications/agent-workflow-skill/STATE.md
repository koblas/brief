# agent-workflow-skill — current state

Scenarios complete: SCENARIO-01..08 (all eight scenarios in the spec). Last updated by the
final product-vision SHIP WITH CHANGES fix pass (fix pass 4).

## Binding decisions
- The skill is a separate `host.Host.Skills() []File` list, never an entry in `Plugin()`. `File.RelPath` is install-root-relative; the skill's own entry is `host.WorkflowSkillDir + "/SKILL.md"`. Setup report kind `setup.KindSkill = "skill"`; artifact kind `artifact.KindSkillWorkflow`. Init row order: config, feature-root, plugin…, hook, skill, agent…, bound-agent…, snippet. Uninstall row order is the strict reverse: snippet, bound-agent…, agent…, skill, plugin…, config. brief owns `host.PluginDir` and below **and** `host.WorkflowSkillDir`; prune stops at each. (SCENARIO-01, S07, S08)
- Older-render upgrade is generic in `setup` (`OriginOlder` → `ActionMerged`/"updated", `verifyFileUnchanged`-guarded, removed like current on Uninstall without `--force`). `renderAgent` takes optional `skills []string`; reviewer never gets `skills:` (R15). (SCENARIO-02)
- Binding classification lives in `internal/platform/agentfile`: `ResolveBinding`, `(Binding).LackingSkill`, `Find` (frontmatter `name:`, project shadows user, no filename fallback), `Parse` (bytes-level twin of `Load`, shares `decode`). Doctor and setup may import only `internal/platform/*`. (SCENARIO-03/06/07)
- `--edit-agents` and Uninstall's own removal **share one target selector**, `boundAgentTargets(root, home, roles)` (`internal/setup/bound_agent.go`): every bare-name planner/implementer binding's own `ScopeProject` `agentfile.Definition`, deduped by path, planner-then-implementer order. `addWorkflowSkill`/`removeWorkflowSkill` are exact inverses over one frontmatter `skills:` key — insert/block-list/flow-list shapes only; every other shape (`shapeOther`, or a quoted/commented/multi-line value, **or a missing opening/closing `---` delimiter — `strings.Cut`'s own `found` is checked, never discarded, so neither function fabricates a closing delimiter**) is untouched, `ActionKept` on add, **no row at all** on removal. Neither is trusted on its own: `planBoundAgent`/`planBoundAgentRemoval` re-decode the edited bytes (`agentfile.Parse`) and require `boundAgentEditVerified`/`boundAgentRemovalVerified` — Name unchanged, Skills exactly original±the skill — before applying; a mismatch falls back to the unedited `ActionKept`/no-row path. Emptying a block or flow list to zero items **drops the `skills:` key line**, never `skills: []`, including via a trailing comma. A non-regular leaf or an escaping resolved path (`.claude` symlinked outside root) gets no row either way, at the artifact/edit level — **ruled accepted**, stays silent there (see below for the separate, unrelated missing-skill *report*). **Every bound-agent read-for-verify and write goes through one type, `confinedAgentFile{resolvedRoot, rel, displayPath}`** — `boundAgentArtifact.agentFile()` is the single construction site; `.write` preserves the leaf's own Lstat'd mode. `verifyBoundAgentUnchanged` classifies a non-`NotExist` read error via `boundAgentPathEscaped` into `*RefusalError{ErrConcurrentEdit}` only when `EvalSymlinks` itself fails with not-exist; the `brief init --edit-agents` rerun copy on that refusal is **ruled accepted**. (SCENARIO-07, SCENARIO-08, fix passes 1-3)
- Uninstall's bound-agent rows (`planBoundAgentRemovals`) are planned only when the brief-workflow skill's own row is **not** `ActionKept`. Roles come from `config.Inspect(nearest)`; a bound-agent row is `ActionRemoved` but **rewritten in place**, landing in `Result.Modified`, never `Result.Removed`. Row order among bound-agent rows is implementer-before-planner, the strict reversal of `--edit-agents`' own planner-first walk — **ruled accepted, that reversal is Uninstall's order rule generally** (S08 (4)). S08 (1)-(3) also **ruled accepted as implemented**: an unremovable/escaping bound agent gets no row on Uninstall either; Uninstall has no writability pre-check for the bound-agent rewrite (unlike Init's R10); an original `skills: []` is lost on round trip rather than restored. (SCENARIO-08)
- Doctor's `roles-skill` OK detail names only the roles it actually checked (resolved, not bound to another plugin), planner-then-implementer: `planner, implementer preload brief-workflow` for two, `<role> preloads brief-workflow` for one, plus `; not verified: <role>` per other-plugin role; SKIP detail is `no bound planner or implementer brief can check`. `init`'s own missing-skill report is a **separate surface, deliberately bare-name-only (ruled accepted)**, unlike roles-skill; its header appends `, or rerun with --edit-agents:` only when this run did not already carry the flag and at least one listed row is one it could still reach (`missingSkillFixableByEditAgents`: a ScopeProject row not escaping root); an escaping ScopeProject row renders `; outside the repository, edit by hand` in text (JSON `scope` stays `"project"`, no escaped field exposed). Stderr block order is **ruled**: `roles_to_add` → flag outcomes (nothing-to-edit) → remaining manual work (missing-skill) → summary (next-action), each own block project-scope rows before user-scope. host-skill's "not a regular file" ERROR Fix is `remove .claude/skills/brief-workflow/SKILL.md, then run 'brief init'` (a plain re-run alone cannot clear it — host-hook keeps the old, ineffective fix; see Open debts). `uninstallLong` names the "brief-workflow" skill directory alongside the plugin and its "left in place" clause reads "brief's own directories". (product-vision SHIP WITH CHANGES fix pass)

## Left unbuilt
None — all eight spec'd scenarios are implemented.

## Traps
- `filepath.WalkDir` does not walk a symlinked directory as its own root — only a symlinked leaf `.md` file is read through its link. A test needing "resolves via `Find` but escapes root" must symlink an ancestor of `agents`, not `agents` itself.
- R10's writability pre-check (`checkWritable`) Lstats a symlinked `.claude` directly and always reports "not a directory" — Init-side tests exercising a symlinked `.claude` for another reason must use `DryRun` or pre-materialize the file. **Uninstall has no writability pre-check at all** (by design — see Binding decisions).
- Every bare-name-role test must inject home (`WithHomeDir` / `withSetupOpts`) — `cli.Run`'s exported tests cannot; use the `run()` seam. A home directory under a *sibling* `t.TempDir()` sits outside root, so the escape check alone excludes it — a test isolating the `ScopeProject` filter specifically must nest home *inside* root.
- `agentfile` imports `internal/platform/host`; neither `host` nor `artifact` may import `agentfile` (cycle).
- `apply`/`writableTargets`/`printArtifacts` (Init) and `applyUninstall` (Uninstall) all take a `boundAgentArts` parameter — every call site, including white-box tests, must pass one (`nil` where irrelevant).
- R16's byte identity on the `--edit-agents` → Uninstall round trip holds only for "no key", a non-empty block list, and canonical `skills: [a]`. An original `skills: []` loses its line entirely (ruled), and `skills:[a]`/`skills:  [a]` normalizes to `skills: [a]` — do not "fix" either without a ruling.
- A dangling symlink at a bound-agent path never reaches `planBoundAgent`/`planBoundAgentRemoval` through Init's or Uninstall's own real flow — `agentfile.Find` filters it out of `boundAgentTargets` first. Testing that guard means calling `planBoundAgent`/`planBoundAgentRemoval` directly (white-box), not through `srv.Init`/`srv.Uninstall`.
- `setup.agentsMissingSkill`'s own `Escaped` field (product-vision fix pass) is computed independently of `boundAgentTargets`' own escape check — both call `filepath.EvalSymlinks` against root and the candidate path, but neither shares code with the other; a future change to one's escape semantics must be mirrored in the other by hand.

## Open debts
- `setup.planSnippet`/`planSnippetRemoval` still treat an `OriginOlder` snippet as "edited locally". **Unowned — dies unless re-opened**.
- host-hook's own "not a regular file" ERROR row still uses the plain, ineffective `run 'brief init'` Fix — the same defect host-skill had before this round, left unfixed here on the finding's own instruction. **Unowned — dies unless re-opened.**
- UNVERIFIED (product-vision item 8): whether a plugin agent (`.claude/skills/brief/agents/*.md`) resolves a *project* skill by bare name `brief-workflow` — no live Claude Code session to check. **RULED: ships UNVERIFIED — disclose in the PR description.**
- DEFERRED (fix pass 1, MINOR, declined as not cheap): `bound_agent.go`'s own frontmatter-delimiter scan duplicates `stepfile`'s private convention rather than a shared exported seam; `planBoundAgent`/`planBoundAgentRemoval` duplicate their ~25-line read prologue; `doctor.Diagnose` re-resolves planner/implementer bindings separately for `rolesCheck` and `rolesSkillCheck`; several test files each hand-roll a row-finder loop instead of a shared helper; `doctor/host_test.go` and `cli/init_internal_test.go` are large enough to warrant splitting. **Unowned — dies unless re-opened.**
- DEFERRED (fix-pass cap reached after pass 3; none changes an exit code or a written file): **[MAJOR, test]** `boundAgentPathEscaped`'s EvalSymlinks-not-exist branch (displayPath gone after a dir swap) is untested. **[MINOR, test]** `Test_verify_bound_agent_unchanged_does_not_misclassify_a_permission_error` lacks the `os.Geteuid() == 0` skip the package uses elsewhere. **[MINOR, test]** two `removeWorkflowSkill` rows have no `skills:` line, so they don't pin the `openLen`/`found` guard their names claim. **[MINOR, correctness]** `confinedAgentFile.read`/`bound_agent.go:964` classify a vanished root only via a second `EvalSymlinks` call, with a doubled error-wrap prefix. **[MINOR, correctness]** `boundAgentPathEscaped` doesn't treat `ENOTDIR` as changed. **[NIT, test]** a stale test name, decoy fixtures, one mis-wrapped doc comment. **[MINOR, arch]** move the skills-list editor into `internal/platform/agentfile`. **Unowned — dies unless re-opened.**
