# agent-workflow-skill — current state

Scenarios complete: SCENARIO-01..06. Last updated by SCENARIO-06.

## Binding decisions
- The skill is a separate `host.Host.Skills() []File` list, never an entry in `Plugin()` — `Plugin(false)` would trim it instead of the hook and break doctor's `host-plugin` check. `File.RelPath` is install-root-relative; the skill's own entry is `host.WorkflowSkillDir + "/SKILL.md"`. Setup report kind `setup.KindSkill = "skill"`; artifact kind `artifact.KindSkillWorkflow`. Row order: config, feature-root, plugin…, hook, skill, agent…, bound-agent…(S07), snippet, roles, roles-skill. brief owns `host.PluginDir` and below **and** `host.WorkflowSkillDir`; prune stops at each. Skill arts ride `writeArts`. Uninstall's `--force` removes an edited SKILL.md through `planPluginRemoval` (Rule 8). (SCENARIO-01)
- Older-render upgrade is **generic in `setup`**: `planPluginFile` classifies `OriginOlder` as `ActionMerged`/"updated"; `apply` re-verifies via `verifyFileUnchanged` before writing; `planPluginRemoval` treats `OriginOlder` like `OriginCurrent`; `writableTargets` includes `ActionMerged` rows (R10). Only planner/implementer have a non-empty older-digest list today. `renderAgent` takes optional `skills []string` (block-list form, after `tools`); reviewer never gets `skills:` (R15). Plain `brief init` (no `--with-agents`) never plans agent files, so it never upgrades an older one either — existing behavior, do not "fix". (SCENARIO-02)
- Binding classification and the lacks-skill decision live in `internal/platform/agentfile`: `ResolveBinding(root, home, value) Binding{Kind, State, Path, Defs}` (Kind: `BindingBare`/`BindingBrief`/`BindingPlugin`; State: `BindingUnbound`/`BindingResolved`/`BindingUnresolved`/`BindingUnverified`) and `(Binding).LackingSkill(skill) []Definition` — the single decision point doctor's roles/roles-skill rows and setup's own missing-skill report both call, since setup cannot import doctor. Doctor and setup may import only `internal/platform/*`; S07–S08 reuse `ResolveBinding`/`LackingSkill` the same way S06 does. doctor's `resolveRoleBinding` is now a thin wrapper: resolve home, call `agentfile.ResolveBinding`. `Find(root, home, name) []Definition` still matches Claude Code's own resolution: frontmatter `name:` anywhere under `.claude/agents/`, recursive, project shadows user entirely, no filename fallback. `brief:<name>` stays filename-based (`<root>/.claude/agents/<name>.md` overriding `<root>/.claude/skills/brief/agents/<name>.md`, stat-only, no frontmatter needed) — Rule 5 is scoped to bare names; `LackingSkill` synthesizes one Definition via `Load` for a `brief:*` binding, zero Frontmatter on a load failure. (SCENARIO-03, SCENARIO-06)
- roles' duplicate WARN is project scope only (`<rel>` root-relative, slash-separated); the `; user-level: <role>` OK suffix sits beside `; not verified: <role>`. `doctor`'s `host-skill` row (path `host.WorkflowSkillDir + "/SKILL.md"`, between `host-hook` and `host-snippet`) is `hostSkillCheck`/`hostSkillRow`: not an install signal — `anyIntegrationFilePresent` stays Plugin(true) ∪ Agents() only (Rule 1); missing WARN keys on `filesInstalled`, not `integrationInstalled`; ERROR "not a regular file" is Rule 7's one new ERROR; older/edited/current route through `originRow` (single-subject, suffix `""`). (SCENARIO-03, SCENARIO-04)
- `agentfile.Frontmatter` carries `Skills []string`/`OmitClaudeMd bool`, decoded loosely: a private `rawFrontmatter` types both as `yaml.Node`, mapped by `frontmatterSkills`/`frontmatterOmitClaudeMd` — a malformed value never fails the whole decode and drops the agent out of Rule 5. `agentfile.Load(path)` shares this decode as the single-file entry point. (SCENARIO-05)
- `roles-skill` considers **planner and implementer only** (reviewer excluded, R15). SKIP `no planner or implementer bound` (Fix nil) when neither is bound+resolved — `agentfile.BindingUnverified` does not count as resolved, so two other-plugin bindings SKIP. A bare-name binding WARNs if **any** of `res.Defs` lacks the skill (Claude Code loads duplicates in unspecified order); the omitClaudeMd suffix follows the same "any" rule. A `brief:*` binding with no/unparseable frontmatter counts as lacking (WARN), never SKIP. OK detail is the fixed string `planner, implementer preload brief-workflow` regardless of which role actually resolved — the roles row already WARNs an unbound/unresolved one. (SCENARIO-05)
- init's own missing-skill report (`setup.Result.AgentsMissingSkill`, unexported `agentsMissingSkill`) covers **bare-name planner/implementer bindings only** (`Kind == agentfile.BindingBare`, never `len(Defs) > 0` — `LackingSkill` synthesizes a Definition even for `brief:*`) — never `brief:*`, other-plugin, or reviewer — computed only when the resolved host is claude-code, from `planConfig`'s post-run `cfg.Roles`, before the DryRun/Print return, never nil (empty `[]MissingSkillAgent{}` otherwise, `Uninstall`'s Result too). One item per lacking `Definition` per role, planner before implementer, Find's own lexical order within a role; a duplicate path across roles is listed twice. Stderr order: `roles_to_add` block, then the missing-skill block, then next-action; neither renders under `--print`. JSON `agents_missing_skill` follows `roles_to_add`, always present (`[]` when empty), item keys exactly `role`,`agent`,`path`,`scope` in that order. (SCENARIO-06)

## Left unbuilt
- `uninstallLong` does not yet mention the skill — S08's addition.
- `bound-agent` (S07's new `setup.Kind`) needs a case in `setup.printArtifacts`' `exhaustive` switch over `setup.Kind`; `artifact.Render`/`digestsFor` switch over `artifact.Kind` instead, needing one only if S07 adds an artifact Kind too. R10's unwritable refusal is exit **1**, not 2 — S07's writability check must match.
- `skills:` shape classification (no key / block list / flow list / other / non-regular) for editing — S07's `--edit-agents` shape table; S05 exposes only the decoded list, never the shape.
- `--edit-agents` flag itself, and the `initLong` sentence `--edit-agents adds it to those agents' "skills:" lists, …` — S07. Its target set uses the same `agentfile.BindingBare` filter S06's report uses.
- S07 must compute the missing-skill report *after* planning its own edits and subtract the agents it will edit, including under `--dry-run` ("after the planned run" — already ruled copy in specification.md).

## Traps
- `golangci-lint`'s `exhaustive` is on: a `switch` over `artifact.Origin` needs all three cases explicit, not just `default`. A test-body `switch` over `setup.Action` trips the same linter; use an if-chain.
- `sha256.Sum256(AgentPlanner())` in an `older…Digests` list duplicates the current digest and makes `OriginOlder` unreachable while every render-pinned test stays green. `olderSkillWorkflowDigests` still ships empty; `host-skill`'s own OriginOlder arm is reachable only through `host_internal_test.go`'s white-box `hostSkillRow` call.
- The Gherkin reads `skills: [brief-workflow]` (flow form); brief's own agent renders are a block list. S07's `--edit-agents` shape table must accept *both* forms on a repo's own agent file — only brief's own renders are fixed to the block form.
- yaml.v3 partially fills fields on a `*yaml.TypeError`. "Any Unmarshal error → not a candidate" (Rule 5) is pinned, so `agentfile.findIn`/`Load` decode `skills:`/`omitClaudeMd:` through `yaml.Node`, never a Go-typed field, or a malformed value drops the whole file (and, downstream, flips roles-skill to SKIP instead of WARNing).
- `filepath.WalkDir` does not descend into symlinked directories, but a symlinked `*.md` *file* is read through its link, so `Definition.Path` can resolve outside the repository. S07's Rule 3 ("never edits a file outside the repository") must check the resolved target, not just `Scope`.
- The `brief:reviewer` override fixture in `Test_diagnose_classifies_roles` has no frontmatter on purpose — pins the filename-based `brief:*` override, unaffected by Rule 5.
- User-level duplicate agent definitions resolve silently; nothing WARNs. UNRULED — final product-vision decides.
- Doctor's row order is `host-plugin, host-hook, host-skill, host-snippet, host-agents, roles, roles-skill`. STATE.md's **init report** order (`hook, skill, agent…, snippet`) is a different list — don't copy it into doctor. Every fixture that writes a "full install" (`newHealthyDoctorFixture`, `newFullyInstalledDoctorFixture`) must also write `h.Skills()`, or the all-OK tests go red on a WARN. `host-skill` checks only `h.Skills()`'s first entry. Never route the skill through `hostHookCheck` or a shared helper with it — the missing-arm copy and fix differ.
- Since S06, a claude-code `Init` call always resolves planner/implementer via `agentfile.ResolveBinding`, so every setup/cli test binding a bare-name role must inject home (`WithHomeDir` / `withSetupOpts`), or it reads the developer's own `~/.claude/agents`. `cli.Run` exported tests cannot inject home; use `init_internal_test.go`'s `run()` seam.
- The header's "or rerun with --edit-agents" is ruled copy (Surface & Copy) even though the flag lands in S07 — do not trim it.
- `agentfile` imports `internal/platform/host` for `PluginDir`. Neither `host` nor `artifact` may ever import `agentfile`, or an import cycle results.

## Open debts
- `setup.planSnippet`/`planSnippetRemoval` still treat an `OriginOlder` snippet as "edited locally". **Unowned — dies unless re-opened**; the next snippet byte change must fix it first.
- `--edit-agents`, the missing-skill *edit* path (the report itself is built), and `bound-agent` rows — S07.
- "File untouched" (S06) is unfalsifiable today — no agent write path exists yet. S07 must add the control arm: the same fixture run with `--edit-agents` that changes the file.
- `uninstallLong`'s skill-removal sentence and the bound-agent uninstall row — S08.
- UNRULED COPY: R11's `merged <rel> (updated)` row and reusing `ActionMerged` for an older-file upgrade were the developer's own call, not Surface & Copy. Owner: final product-vision, before SHIP.
- UNRULED COPY: roles-skill's OK detail stays the fixed `planner, implementer preload brief-workflow` even when one of the two is unbound/unresolved. Owner: final product-vision, before SHIP.
- UNRULED: init's own missing-skill report deliberately covers bare-name bindings only, while doctor's roles-skill also WARNs on `brief:*`. Owner: final product-vision.
- UNRULED: init's stderr rendering (`missingSkillLines`) groups rows by scope — every project row before every user row — to reconcile the plan's own literal example (project line before user line) with its stated `Result`/JSON role-major order (planner before implementer). Surface & Copy never states a display-order rule; this grouping was the developer's own call. Owner: final product-vision, before SHIP.
- UNVERIFIED (product-vision item 8): whether a plugin agent (`.claude/skills/brief/agents/*.md`) resolves a *project* skill by bare name `brief-workflow` — no live Claude Code session to check. Verify before release — unowned; the final gate decides whether this blocks SHIP.
